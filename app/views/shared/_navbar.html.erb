<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container">
    <h1 class="navbar-brand fs-2">The RialNews</h1>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="navbar-nav ms-auto mb-2 mb-lg-0">
      <%= link_to "Home", root_path, class: "nav-link active" %>
      <% if user_signed_in? %>
        <div class="btn-group">
          <%= link_to 'Cerrar sesión', destroy_user_session_path, class: 'btn  nav-link active', method: :delete %>
          <%= link_to 'editar', edit_user_registration_path, class: 'btn  nav-link active' %>
          <a href="", class=' nav item nav-link text-primary'> [  <%= current_user.email %>  ]</a>


        </div>
      <% else %>
        <div class="btn-group">
          <%= link_to 'Iniciar sesión', new_user_session_path, class: 'btn nav-link active ps-3' %>
          <%= link_to 'Registro', new_user_registration_path, class: 'btn  nav-link active ps-3 pe-5' %>
        </div>
      <% end %>
    </div>
  </div>
</nav>
